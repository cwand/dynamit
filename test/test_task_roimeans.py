import unittest
# import dynamit
# import os
# import xmltodict
# import numpy as np


class TestTaskROIMeans(unittest.TestCase):

    '''
    def test_task_simple(self):
        f = open(os.path.join('test', 'xml_input', 'test_roi_means.xml'))
        tree = xmltodict.parse(f.read(), xml_attribs=True)
        task = tree['dynamit1']['task']
        dynamit.task_roi_means(task)
        with open(os.path.join('test', 'out.txt'), 'r') as res_file:
            header = res_file.readline()
            self.assertEqual(header, "# 0   1   2")
        data = np.loadtxt(os.path.join('test', 'out.txt'))


    def tearDown(self):
        os.remove(os.path.join('test', 'out.txt'))
    '''
